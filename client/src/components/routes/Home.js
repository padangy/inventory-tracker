import Layout from '../shared/Layout'

const Home = () => {
  return (
  <Layout>
    <h4>Welcome to the items app!</h4> {/* h4 tag is the children of Layout tag */}
    </Layout>
  )
}

export default Home